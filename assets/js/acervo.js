const grid=document.getElementById("acervosGrid"),filterButtons=document.querySelectorAll("[data-filter]");let acervos=[];function escapeHTML(v=""){return String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}function renderAcervos(filter="todos"){const items=filter==="todos"?acervos:acervos.filter(i=>i.tipo===filter);grid.innerHTML=items.map(i=>`<a class="acervo-card" href="./galeria.html?id=${encodeURIComponent(i.id)}"><div class="card-cover">${escapeHTML(i.titulo)}</div><p class="kicker">${escapeHTML(i.tipo)}</p><h3>${escapeHTML(i.titulo)}</h3><p>${escapeHTML(i.descricao)}</p></a>`).join("")}filterButtons.forEach(b=>b.addEventListener("click",()=>{filterButtons.forEach(x=>x.classList.remove("active"));b.classList.add("active");renderAcervos(b.dataset.filter)}));fetch("./data/acervos.json").then(r=>r.json()).then(d=>{acervos=d;renderAcervos()});